# Copy to .env.local (gitignored). Values below are local dev defaults from `supabase status`.
PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
PUBLIC_SUPABASE_ANON_KEY=<paste publishable key from `supabase status` output>
PUBLIC_VAPID_KEY=<paste VAPID public key — generate with: npx web-push generate-vapid-keys>
